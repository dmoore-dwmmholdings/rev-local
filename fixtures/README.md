# fixtures

Offline git and Subversion fixture repositories, the mock engine and the mock MCP
server (SPEC §16.2). Nothing here touches the network or spends model tokens.

Scaffolded by `RL-101`; generators land in `RL-201`–`RL-205`.
